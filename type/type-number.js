// tipo Number

const myNumber = 3;

const fstNumber = 1;
const secNumber = 2;

const mathOperation = fstNumber - secNumber;

console.log(mathOperation);

// ponto flutuante

const numFloatingPoint = 3.3;

const floatingPointWthOutZero = 0.5;

const newMathOperation = fstNumber / numFloatingPoint;

console.log(newMathOperation);

// Nan => Not a Number

const ariane = "Ariane";

const otherMathOperation = fstNumber + ariane;

console.log(otherMathOperation);
